# MagicSquare — TDD QA Coverage Analysis Report

## 1. 작업 개요

| 항목 | 내용 |
|---|---|
| **프로젝트** | MagicSquare_XX (4×4 Magic Square, 빈칸 2개) |
| **문서 ID** | RPT-19-TDD-QA-COVERAGE-ANALYSIS |
| **일자** | 2026-05-29 |
| **TDD 단계** | **Ask mode** — QA 커버리지 분석 only (production 코드 수정·추가 **금지**) |
| **목적** | Step 0 기준선(pytest·RED·GM) 확정, Dual-Track 커버리지 SSOT 측정, NFR-01/02/03 게이트 판정 |
| **기준 문서** | `.cursorrules`, `docs/test_plan.md` §6·§7, `docs/PRD_MagicSquare.md` (NFR-01~03), RPT-02 (Dual-Track I1~I11), RPT-18 (REFACTOR Phase 0) |
| **선행 산출** | FR-01~05 GREEN, GM 6/6, RPT-18 RF-07 완료 |

## 2. 사용자 요청 이력(요약)

1. **TDD Phase — Ask mode** QA 커버리지 분석 (production 수정 금지)
2. ECB 경로 매핑(가상명 → `src/` 실경로) 기준 Step 0·Step 1 실행
3. Report / Prompt Transcript Export (본 보고서)

## 3. ECB 경로 매핑 (분석 시 적용)

| 프롬프트 관례 | 실제 경로 | 레이어 |
|---|---|---|
| `domain.py` | `src/control/use_cases/solve_partial_magic_square.py` | Control |
| Domain Logic | `src/entity/rules/*.py`, `src/entity/errors.py` | Entity |
| `boundary.py` | `src/boundary/validator.py`, `src/boundary/errors.py` | Boundary |
| `gui` | `src/boundary/screen/app.py`, `grid_input.py`, `__main__.py` | Screen (Boundary 하위) |

## 4. Step 0 — 기준선 (실측)

### 4.1 전체 pytest

```bash
python -m pytest -q
```

| 항목 | 결과 |
|---|---|
| PASS | **64** |
| FAIL | **0** |
| ERROR | **0** |
| 소요 | ~0.23s |

### 4.2 RED 스켈레톤 (`pytest.fail`)

| 검색 | 결과 |
|---|---|
| `pytest.fail("RED:` | **0건** — Report/10 Skeleton 24건은 assert 전환 완료 (RPT-17/18 동일) |
| `pytest.fail(` 잔존 | **1건** — `tests/boundary/test_validator_shape_ac_fr_01_01.py:88` (TypeError 방어; 스켈레톤 아님) |

**프롬프트 “19 RED”:** 현재 **실패 RED 0건**. 이력상 FR-01~05 RED ID는 **24건** (`*_red.py` 7파일)이며 전부 GREEN.

| REFACTOR gate (RED skeleton) | 판정 |
|---|---|
| 의도 `pytest.fail` 스켈레톤 잔존 | **충족** (미충족 아님) |
| 전체 pytest GREEN | **충족** (64/64) |

**별도 미충족 (RPT-18 Phase 0):** `tests/boundary/test_main_window.py` **미생성 (G-05)** — Screen/RF-06 선행.

### 4.3 GM-1 (Golden Master)

| 명령 | 결과 |
|---|---|
| `tests/test_gm_01_magic_square_golden_master.py` | **파일 없음** (exit 4) |
| `tests/test_golden_master_magic_square.py` (**SSOT**) | **6 passed** — GM matched |

## 5. Step 1 — Dual-Track 커버리지 (SSOT)

환경: Python 3.13.13, pytest 8.4.2, pytest-cov 7.0.0

### 5.1 Domain Track — NFR-01 ≥ 95%

```bash
python -m pytest tests/entity/ tests/control/ \
  --cov=src/entity --cov=src/control \
  --cov-report=term-missing
```

| 지표 | 값 | Gate |
|---|---|---|
| **합계** (`entity`+`control`) | **95%** (83 stmts, 4 miss) | **PASS** (경계값) |
| 수집 테스트 | 30 passed | — |

| 모듈 | Cover | Missing |
|---|---|---|
| `solve_partial_magic_square.py` | 100% | — |
| `blank_finder.py`, `missing_number_finder.py`, `entity/errors.py` | 100% | — |
| `resolve_magic_square.py` | 87% | 26, 29 (`_run_domain` success path) |
| `magic_square_validator.py` | 93% | 41, 43 (anti-diagonal 분기) |

### 5.2 Boundary Track — NFR-02 ≥ 85%

```bash
python -m pytest tests/boundary/ \
  --cov=src/boundary \
  --cov-report=term-missing
```

| 범위 | Stmts | Miss | Cover | NFR-02 |
|---|---|---|---|---|
| `validator.py` + `errors.py` | 38 | 0~1 | **97~100%** | **PASS** |
| `screen/` (`app`, `grid_input`, `__main__`) | 118 | 118 | **0%** | 미측정 |
| **`src/boundary` 패키지 전체** | 156 | 118~119 | **~24%** | **FAIL** |
| 수집 테스트 | 28 passed | — | — | — |

> pytest-cov가 import 체인으로 `control`/`entity`를 함께 표시할 수 있음. NFR-02 판정은 **`src/boundary` 패키지** 기준으로 해석한다.

### 5.3 전역 gate — NFR-03 ≥ 80%

```bash
python -m pytest --cov=src --cov-report=term-missing
```

| 지표 | 값 | Gate |
|---|---|---|
| **`src/` 전체** | **49%** (239 stmts, 122 miss) | **FAIL** |
| 수집 테스트 | 64 passed | — |

**주 원인:** `src/boundary/screen/*` **118 lines 0%** (전체 miss의 대부분).

## 6. NFR 게이트 요약

| NFR | 목표 | 실측 | 판정 |
|---|---|---|---|
| **NFR-01** Domain (`entity`+`control`) | ≥ 95% | **95%** | **PASS** |
| **NFR-02** Boundary (`src/boundary`) | ≥ 85% | validator/errors **97~100%**; 패키지 전체 **~24%** | **조건부** — Screen 제외 시 PASS |
| **NFR-03** Global (`src`) | ≥ 80% | **49%** | **FAIL** |

## 7. 테스트 ↔ 레이어 매핑

| Track | 테스트 | Production | 건수 (PASS) |
|---|---|---|---|
| Domain | `tests/entity/`, `tests/control/` | `src/entity/*`, `src/control/*` | 30 |
| Boundary | `tests/boundary/` | `validator`, `errors` (+ screen 미커버) | 28 |
| Golden Master | `tests/test_golden_master_magic_square.py` | solver 회귀 경로 | 6 |
| User scaffold | `tests/entity/test_user.py` | (cov 리포트 미포함) | 4 |

### 7.1 FR-01~05 RED ID 테스트 (`*_red.py`)

| 파일 | ID 수 | 상태 |
|---|---|---|
| `test_d_loc_red.py` | 1 (D-LOC-01) | GREEN |
| `test_d_mis_red.py` | 1 (D-MIS-01) | GREEN |
| `test_d_val_red.py` | 7 (D-VAL-01~06) | GREEN |
| `test_d_sol_red.py` | 4 (D-SOL-01~04) | GREEN |
| `test_u_flow_02_red.py` | 3 (U-FLOW-02) | GREEN |
| `test_u_in_red.py` | 5 (U-IN-04~08) | GREEN |
| `test_u_out_red.py` | 3 (U-OUT-01~03) | GREEN |
| **소계** | **24** | **전부 PASS** |

### 7.2 AC-FR-01-01 shape / isolation

| 파일 | 건수 | 상태 |
|---|---|---|
| `test_validator_shape_ac_fr_01_01.py` | 20 | GREEN |
| `test_resolve_shape_guard_ac_fr_01_01.py` | 10 | GREEN |
| **소계** | **30** | **전부 PASS** |

## 8. 미커버·갭 분석 (권장 후속 — 분석만)

| 갭 | 파일·라인 | 권장 조치 (구현 세션) |
|---|---|---|
| Anti-diagonal | `magic_square_validator.py` 41, 43 | D-VAL anti-diagonal 실패 그리드 1건 |
| Control success path | `resolve_magic_square.py` 26, 29 | success orchestration spy 테스트 1건 |
| Screen GUI | `screen/app.py`, `grid_input.py` | G-05 `test_main_window.py` 또는 `tests/boundary/screen/` |
| GM 경로 alias | `test_gm_01_*.py` | 문서·프롬프트를 `test_golden_master_magic_square.py`로 SSOT 정렬 |

## 9. 생성·수정 파일

| 구분 | 경로 |
|---|---|
| 생성 | `Report/19.MagicSquare_TDD-QA-Coverage-Analysis_Report.md` (본 파일) |
| 생성 | `Prompt/19.MagicSquare_TDD-QA-Coverage-Analysis_Prompt-Transcript.md` |
| **미수정** | `src/**` (Ask mode — production 변경 없음) |

## 10. 추적성 체인

```
RPT-18 REFACTOR Program Session (Phase 0 G-01~G04 ✅, G-05 ❌)
  → Ask mode QA Coverage Analysis (본 문서)
      · Step 0: 64 PASS, RED skeleton 0, GM 6/6
      · Step 1: NFR-01 PASS, NFR-02 조건부, NFR-03 FAIL (Screen)
  → (다음) G-05 Screen test RED→GREEN · NFR-03 80% gate
```

## 11. 품질 게이트 준수 체크

| 규칙 | 준수 |
|---|---|
| Ask mode — production 코드 미변경 | ✅ |
| `pytest` 전체 GREEN 유지 | ✅ (64/64) |
| GM matched | ✅ (6/6) |
| NFR-01 Domain ≥95% | ✅ (95%, 경계) |
| NFR-02 Boundary 패키지 전체 ≥85% | ❌ (Screen 0%) |
| NFR-03 Global `src` ≥80% | ❌ (49%) |
| RED `pytest.fail` 스켈레톤 잔존 | ✅ 없음 |

## 12. 산출물 Export

| 유형 | 경로 |
|---|---|
| Report | `Report/19.MagicSquare_TDD-QA-Coverage-Analysis_Report.md` |
| Transcript | `Prompt/19.MagicSquare_TDD-QA-Coverage-Analysis_Prompt-Transcript.md` |

---

*본 보고서는 TDD Ask mode QA 커버리지 분석(Step 0·Step 1) 실측 결과를 고정한다. production 변경·커버리지 개선 구현은 후속 세션으로 분리한다.*
