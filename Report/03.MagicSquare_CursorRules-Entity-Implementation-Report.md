# MagicSquare — Cursor Rules and Entity Implementation Report

## 1. 작업 개요
- 프로젝트: MagicSquare
- 목표: Cursor Rule 설계/정의 및 ECB 기반 `User` 엔티티 + pytest 테스트 구현
- 범위:
  - `.cursorrules`/`.cursor/rules` 전략 검토
  - `.cursorrules` YAML 섹션 설계 및 상세화
  - `entity` 레이어 `User` 클래스 작성
  - pytest(AAA) 테스트 작성 및 실행 검증

## 2. 사용자 요청 이력(요약)
1. Cursor Rule 구조 설계 설명 요청
2. `.cursor/rules` 및 `.cursorrules` YAML 뼈대 생성
3. `tdd_rules` 세부화
4. `.cursorrules` 리뷰(오류/누락/충돌 점검)
5. 빈 섹션 전체 채우기
6. `.cursorrules` 기준으로 ECB `User` 엔티티 + pytest 테스트 작성
7. Report/Prompt 산출물 export 요청

## 3. 주요 산출물
- 규칙 파일
  - `.cursorrules`
  - `.cursor/rules/magicsquare.yaml`
- 도메인(entity) 코드
  - `src/entity/models/user.py`
- 테스트 코드
  - `tests/entity/test_user.py`
- 패키지 마커
  - `src/__init__.py`
  - `src/entity/__init__.py`
  - `src/entity/models/__init__.py`
  - `tests/__init__.py`
  - `tests/entity/__init__.py`
- 본 보고서
  - `Report/03.MagicSquare_CursorRules-Entity-Implementation-Report.md`

## 4. 규칙 구성 결과
- `code_style`: Python 3.10+, PEP8, type hints, Google docstring, line length 88
- `architecture`: ECB 레이어(boundary/control/entity) 역할 및 의존성 방향 명시
- `tdd_rules`: red/green/refactor를 `description`, `rules`, `must_not`로 세분화
- `testing`: pytest, AAA, coverage 80%, fixture scope, naming convention
- `forbidden`: `print()`, 하드코딩 상수, bare `except` 금지 및 대안 명시
- `file_structure`: ECB 기준 디렉토리 트리 정의
- `ai_behavior`: 코드 작성 전/중/후 규칙 정의

## 5. 구현 상세 (User Entity)
- 레이어 위치: `src/entity/models/user.py`
- 설계 원칙:
  - 외부 I/O 없음(순수 도메인 규칙)
  - 생성 시점 상태 검증(`__post_init__`)
  - 상태 전이 메서드(`activate`, `deactivate`)
  - 이름 변경 규칙(`rename`)
- 검증 규칙:
  - 이름 공백 금지
  - 이메일 기본 형식 검증(`@` 포함 및 시작/끝 금지)

## 6. 테스트 상세 (pytest + AAA)
- 테스트 파일: `tests/entity/test_user.py`
- 케이스:
  - 정상 생성 필드 검증
  - 잘못된 이메일 예외 검증
  - 빈 이름 변경 예외 검증
  - 비활성화 상태 전환 검증
- 패턴: Arrange-Act-Assert 주석 구조로 작성

## 7. 실행/검증 결과
- `pytest -q`: 환경에 직접 명령 미등록(실행 불가)
- `python -m pytest -q`: 실행 성공
  - 결과: `4 passed in 0.04s`
- 정적 진단:
  - 수정 파일 lint 오류 없음

## 8. 준수 체크
- `.cursorrules` 반영 기준으로 구현: 준수
- ECB entity 레이어 경계 준수: 준수
- type hints 적용: 준수
- Google docstring 적용: 준수
- pytest + AAA: 준수
- 테스트 약화/우회 없음: 준수
- `print()` 디버깅 미사용: 준수
