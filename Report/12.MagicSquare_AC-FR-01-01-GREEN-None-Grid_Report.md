# MagicSquare — AC-FR-01-01 GREEN (grid=None / INVALID_SIZE code) Report

## 1. 작업 개요

| 항목 | 내용 |
|---|---|
| **프로젝트** | MagicSquare_XX |
| **문서 ID** | RPT-12-AC-FR-01-01-GREEN |
| **일자** | 2026-05-29 |
| **TDD 단계** | **GREEN** (Track A — Boundary, 1 test slice) |
| **목적** | AC-FR-01-01 `grid=None` → `error.code == INVALID_SIZE` 최소 구현 |
| **기준 문서** | `docs/PRD_MagicSquare.md`, `docs/test_plan.md`, `docs/defect_list.md` |
| **대상 AC** | AC-FR-01-01 — 4×4 외 입력 즉시 실패 (`INVALID_SIZE` / PRD `E_INVALID_SHAPE`) |

## 2. 사용자 요청 이력(요약)

1. 교육용 GREEN 프롬프트를 MagicSquare_XX 프로젝트 구조에 맞게 변환
2. Track A 단일 테스트 GREEN 수행 (`test_none_grid_returns_code_invalid_size`)
3. Report / Prompt Transcript Export (본 보고서)

## 3. GREEN 대상 테스트

| 항목 | 값 |
|---|---|
| 파일 | `tests/boundary/test_validator_shape_ac_fr_01_01.py` |
| 클래스 | `TestAcFr0101NormalFailureReturn` |
| 테스트 | `test_none_grid_returns_code_invalid_size` |
| 입력 | `grid = None` |
| 검증 | `result.error.code == INVALID_SIZE_CODE` (`"INVALID_SIZE"`) |
| 상수 SSOT | `tests/conftest.py` — `INVALID_SIZE_CODE`, `INVALID_SIZE_MESSAGE` |

**의도적 범위 제외:** `[]`, 3×4, 4×3, 5×5, `[[]]*4`, Track B(Control), REFACTOR.

## 4. pytest 실행 결과

### 4.1 구현 전 (RED)

```text
python -m pytest tests/boundary/test_validator_shape_ac_fr_01_01.py::TestAcFr0101NormalFailureReturn::test_none_grid_returns_code_invalid_size -v
```

| 결과 | 내용 |
|---|---|
| **ERROR** | fixture setup — `ModuleNotFoundError: No module named 'src.boundary'` |
| 관련 Defect | DEF-001 (패키지 미생성), DEF-003 (None 분기 미구현) |

### 4.2 구현 후 (GREEN)

| 결과 | 내용 |
|---|---|
| **PASSED** | `1 passed in 0.12s` |

**환경 이슈:** 로컬 Python에 `pydantic` 미설치 → `pip install "pydantic>=2.0,<3"` 후 재실행 (`requirements.txt` 기준).

## 5. 구현 요약

### 5.1 신규 패키지 `src/boundary/`

| 파일 | 역할 |
|---|---|
| `__init__.py` | Boundary 패키지 스캐폴드 |
| `errors.py` | `BoundaryErrorDetail`, `ValidationFailure` (pydantic `BaseModel`) |
| `validator.py` | `BoundaryValidator.validate()` — **`grid is None` 분기만** |

### 5.2 `BoundaryValidator.validate()` — None 분기

```python
if grid is None:
    return ValidationFailure(
        success=False,
        error=BoundaryErrorDetail(
            code=_INVALID_SIZE_CODE,      # "INVALID_SIZE"
            message=_INVALID_SIZE_MESSAGE, # "Grid must be 4x4."
        ),
    )
```

- `grid is not None` → `NotImplementedError` (size 위반 등 선행 구현 금지 준수)
- 메시지·코드는 `tests/conftest.py`와 문자 단위 동일

### 5.3 해소·부분 해소 Defect

| ID | Severity | GREEN 전 | GREEN 후 |
|---|---|---|---|
| DEF-001 | Critical | `src.boundary` 미존재 | **해소** — 패키지·모듈 생성 |
| DEF-003 | Critical | None 분기 미구현 | **부분 해소** — `code` 검증 테스트 1건 GREEN |
| DEF-008 | High | 메시지 상수·팩토리 없음 | **부분 해소** — None 반환 시 message 포함 (해당 테스트 미실행) |
| DEF-009 | Medium | `ValidationFailure` 미정의 | **부분 해소** — pydantic 모델 정의 (타입 테스트 미실행) |
| DEF-010 | Medium | None 시 TypeError | **부분 해소** — None 선행 분기 (해당 테스트 미실행) |

**미해소 (본 GREEN 범위 밖):** DEF-002, DEF-004, DEF-005~007, DEF-011, DEF-012 및 shape suite 나머지 19+ Control 10건.

## 6. 생성·수정 파일

| 구분 | 경로 |
|---|---|
| 생성 | `src/boundary/__init__.py` |
| 생성 | `src/boundary/errors.py` |
| 생성 | `src/boundary/validator.py` |
| 생성 | `Report/12.MagicSquare_AC-FR-01-01-GREEN-None-Grid_Report.md` (본 파일) |
| 생성 | `Prompt/12.MagicSquare_AC-FR-01-01-GREEN-None-Grid_Prompt-Transcript.md` |
| **미수정** | `tests/**`, `src/control/**`, `README.md`, `docs/defect_list.md` |

## 7. 추적성 체인 (본 작업)

```
AC-FR-01-01 → docs/test_plan.md (O-1)
  → tests/boundary/test_validator_shape_ac_fr_01_01.py
    ::TestAcFr0101NormalFailureReturn::test_none_grid_returns_code_invalid_size
  → src/boundary/validator.py (grid is None)
  → src/boundary/errors.py (ValidationFailure)
  → docs/defect_list.md (DEF-001, DEF-003 부분 해소)
```

## 8. GREEN 규칙 준수 체크

| 규칙 | 준수 |
|---|---|
| 최소 구현 (현재 실패 테스트 1건만) | ✅ |
| REFACTOR / 설계 개선 없음 | ✅ |
| Track B(Control) 미구현 | ✅ |
| tests/ 미수정 | ✅ |
| size 위반 분기 선행 구현 없음 | ✅ |
| `.cursor/rules/magicsquare-tdd-testing.mdc` GREEN | ✅ |

## 9. 산출물 Export

| 유형 | 경로 |
|---|---|
| Report | `Report/12.MagicSquare_AC-FR-01-01-GREEN-None-Grid_Report.md` |
| Transcript | `Prompt/12.MagicSquare_AC-FR-01-01-GREEN-None-Grid_Prompt-Transcript.md` |
