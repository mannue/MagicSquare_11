[pytest]
markers =
    boundary: Track A — Boundary / input contract tests
    control: Track B — Control orchestration and domain isolation tests
    entity: Track B — Domain / entity logic tests (no domain mock)
    golden_master: Golden Master / Approval regression tests (GM-TC-*)
